[project]
name = "azure-ai-evaluation-reference-test"
version = "0.0.0"
requires-python = ">=3.12"
dependencies = [
    "azure-ai-evaluation==1.18.5",
    "genai-reference-shared",
]

[tool.uv.sources]
genai-reference-shared = { path = "../../shared", editable = true }

[tool.uv]
package = false
